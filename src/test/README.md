## TCP 서버와 클라이언트 구현 테스트
TCP 서버 클래스와 클라이언트를 구현하여 `ServerSocket` 객체로 연결을 받아들이는 소켓을 생성한 후,
클라이언트로부터 요청을 기다린다.
이후 클라이언트를 실행하여 연결을 요청하면 서버에 하나의 연결이 요청된 뒤 바로 종료된다.   
이를 개선하기 위해 서버에서의 요청을 받아들이는 부분, 즉 `accept()` 메서드를 쓰레드를 이용하여 무한 루프를 돌며 요청을 계속 받아들이고,
요청이 오면 새로운 쓰레드를 생성하여 각 클라이언트들과 통신을 할 수 있도록 개선.